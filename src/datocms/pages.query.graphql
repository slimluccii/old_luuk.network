#import '@datocms/page.fragment.graphql'

query Pages($locale: SiteLocale) {
  _site {
    faviconMetaTags {
      attributes
      content
      tag
    }
  }
  allPages(locale: $locale) {
    ...Page
  }
  footer(locale: $locale) {
      title
      text
  }
}
