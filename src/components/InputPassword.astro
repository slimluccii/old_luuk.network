---
import type { HTMLAttributes } from "astro/types";

type Props = Omit<HTMLAttributes<"input">, "type">;

const { class: className, ...props } = Astro.props;
---

<input {...props} type="password" class:list={["input-email", className]} />

<style></style>
