---
import type { HTMLAttributes } from "astro/types";

type Props = Omit<HTMLAttributes<"input">, "type">;

const { class: className, ...props } = Astro.props;
---

<input {...props} class:list={["input-text", className]} type="text" />

<style></style>
